# OddsPapi 历史赔率回填批次配置
# league_ids 使用 API-Football 联赛 id。
#
# 当前规则：
# - batches 只放“明确准备立即自动回填”的联赛。
# - 已完成、仅补漏、待复测联赛不放入 batches，避免误启动后重复扫库和消耗请求。
# - 待复测联赛必须先通过 1-5 场小样本验证可以写入快照，再重新加入 batches。

batches: []

