# 2026-05-25 Codex 工作日志

## 作者标识约定

作者：Codex（本会话）

如果两边都使用 Codex，建议后续每段日志都写清楚：

- 作者：例如 `Codex（模型侧）`、`Codex（数据回填侧）`、具体开发者名字或机器/分支名。
- 日期：使用自然日期，例如 `2026-05-25`。
- 提交号：已经提交的工作写 commit hash；未提交工作明确写 `未提交`。
- 给对方的话：单独放在“给协作者的话”下面，避免混在实现记录里。

这样即使日志都由 Codex 代写，也可以按“作者/职责/提交号”分辨来源。

## 已提交工作一：统一模型接口与 Dixon-Coles 全局均值模型

提交号：`2694713`

提交信息：`添加Dixon-Coles模型接口`

完成内容：

- 新增统一比分模型入口 `score_model_service.py`。
- 新增 Dixon-Coles 全局均值模型 `DixonColesGoalModel`。
- 推荐层改为通过统一入口消费比分概率分布，后续模型只要输出 `GoalDistributionPrediction` 就能接入亚盘和大小球概率计算。
- CLI 新增 `icewine models train-dixon-coles --limit 1000`。
- 新增设计文档和实现计划：
  - `docs/设计/20260525-Dixon-Coles与统一模型接口设计.md`
  - `docs/计划/20260525-Dixon-Coles与统一模型接口实现计划.md`

验证记录：

- `python -m pytest tests -q`：`173 passed`
- 本次相关文件 `ruff check`：通过

## 已提交工作二：Dixon-Coles 攻防强度模型

提交号：`5712cd7`

提交信息：`添加Dixon-Coles攻防强度模型`

完成内容：

- 新增 `DixonColesAttackDefenseModel`。
- 新增 `DixonColesTeamParameters`。
- 新增 `train_dixon_coles_attack_defense_model(samples)`。
- 模型会拟合：
  - 每队进攻参数
  - 每队防守参数
  - 主场优势
  - Dixon-Coles `rho`
- 未见过球队使用中性攻防参数回退。
- `score_model_service.py` 已适配 `predict_match_goal_distribution(home, away)` 这类需要主客队上下文的模型。
- CLI 新增：
  - `icewine models train-dixon-coles-attack-defense --limit 1000`
- 新增设计文档和实现计划：
  - `docs/设计/20260525-Dixon-Coles攻防强度模型设计.md`
  - `docs/计划/20260525-Dixon-Coles攻防强度模型实现计划.md`

验证记录：

- 目标测试：`18 passed`
- 完整测试：`177 passed`
- 本次触达文件 `ruff check`：通过

## 给协作者的话

- 我这边没有改历史赔率回填规则，也没有改训练样本从赔率表取样的口径。
- 当前新增模型主要依赖历史赛果数据：联赛、主队、客队、比分、时间权重。
- 赔率 24 小时内快照仍建议由数据回填侧继续确定口径；模型侧暂时只提供可插拔的比分概率模型能力。
- 如果你那边确定了历史赔率到训练样本的主盘口/时间点规则，可以直接告诉我字段和口径，我这边再接盘口价值模型或回测层。
