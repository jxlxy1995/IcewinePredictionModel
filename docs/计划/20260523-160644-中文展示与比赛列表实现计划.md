# 中文展示与比赛列表 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 增加中文展示名映射和比赛列表 CLI，便于人工检查同步到的未来比赛。

**Architecture:** 新增 `display_service` 负责英文名到中文展示名映射，新增 `match_query_service` 负责按北京时间窗口查询比赛，CLI 增加 `matches upcoming` 命令。

**Tech Stack:** Python、SQLite、SQLAlchemy、Typer、PyYAML、pytest。

---

## Task 1: 中文展示名映射

**Files:**
- Create: `config/display_names.yaml`
- Create: `src/icewine_prediction/display_service.py`
- Create: `tests/test_display_service.py`

- [ ] 写失败测试：`DisplayNameService` 能把 `Serie A` 显示为 `意甲`，找不到映射时回退原名。
- [ ] 实现 `load_display_names()` 和 `DisplayNameService.display_league()`、`display_team()`。
- [ ] 运行 `C:\ProgramData\anaconda3\python.exe -m pytest tests/test_display_service.py -v`，期望 PASS。
- [ ] 提交：`git commit -m "添加中文展示名映射"`

## Task 2: 比赛查询服务

**Files:**
- Create: `src/icewine_prediction/match_query_service.py`
- Create: `tests/test_match_query_service.py`

- [ ] 写失败测试：构造三场比赛，只返回未来 24 小时内 `scheduled` 比赛，并按开赛时间升序。
- [ ] 实现 `list_upcoming_matches(session, start_time, hours)`。
- [ ] 运行 `C:\ProgramData\anaconda3\python.exe -m pytest tests/test_match_query_service.py -v`，期望 PASS。
- [ ] 提交：`git commit -m "添加未来比赛查询服务"`

## Task 3: CLI 展示比赛列表

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Create: `tests/test_matches_cli.py`

- [ ] 写失败测试：`icewine matches --help` 包含 `upcoming`。
- [ ] 实现 `matches` 命令组和 `matches upcoming --hours 24`。
- [ ] 输出格式：`意甲 2026-05-24 00:00 博洛尼亚 vs 国际米兰`。
- [ ] 运行 `C:\ProgramData\anaconda3\python.exe -m pytest tests/test_matches_cli.py tests/test_cli.py -v`，期望 PASS。
- [ ] 提交：`git commit -m "添加未来比赛列表命令"`

## Final Verification

- [ ] 运行 `C:\ProgramData\anaconda3\python.exe -m pytest tests -v`，期望全部通过。
- [ ] 手动运行 `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m icewine_cli matches upcoming --hours 24`。
- [ ] 合并到 `main` 前确认 `.env` 未进入 git 状态。
