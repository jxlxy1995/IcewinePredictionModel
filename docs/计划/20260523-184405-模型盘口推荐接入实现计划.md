# 模型盘口推荐接入实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将基础 Poisson 赛果模型转换为亚盘和大小球的理论概率，并接入现有推荐预览 CLI。

**Architecture:** 新增独立的盘口概率服务，负责从比分概率矩阵推导亚盘、大小球方向概率；推荐服务复用现有 `Recommendation`、信心等级和手数映射，比较模型概率与赔率隐含概率后生成推荐。CLI 新增模型推荐预览命令，继续使用中文展示比赛与推荐文本。

**Tech Stack:** Python 3.12、Typer、SQLAlchemy、pytest、Decimal、Asia/Shanghai 时间。

---

### Task 1: 比分矩阵与盘口概率服务

**Files:**
- Create: `src/icewine_prediction/market_probability_service.py`
- Test: `tests/test_market_probability_service.py`

- [ ] **Step 1: Write the failing tests**

```python
from decimal import Decimal

from icewine_prediction.market_probability_service import (
    build_score_probability_grid,
    calculate_asian_handicap_cover_probability,
    calculate_total_goals_probability,
)


def test_score_probability_grid_is_normalized():
    grid = build_score_probability_grid(
        home_expected_goals=Decimal("1.50"),
        away_expected_goals=Decimal("1.10"),
    )

    assert grid.max_goals == 8
    assert sum(grid.probabilities.values()) == Decimal("1.0000")
    assert grid.probabilities[(1, 1)] > Decimal("0")


def test_total_goals_probability_handles_half_line():
    grid = build_score_probability_grid(
        home_expected_goals=Decimal("1.80"),
        away_expected_goals=Decimal("1.20"),
    )

    probability = calculate_total_goals_probability(grid, line=Decimal("2.5"), side="over")

    assert probability > Decimal("0.50")


def test_asian_handicap_cover_probability_handles_quarter_line():
    grid = build_score_probability_grid(
        home_expected_goals=Decimal("1.70"),
        away_expected_goals=Decimal("1.00"),
    )

    probability = calculate_asian_handicap_cover_probability(
        grid,
        line=Decimal("-0.25"),
        side="home",
    )

    assert probability > Decimal("0.50")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_market_probability_service.py -q`

Expected: FAIL because `icewine_prediction.market_probability_service` is missing.

- [ ] **Step 3: Write minimal implementation**

Implement:
- `ScoreProbabilityGrid(max_goals, probabilities)`
- `build_score_probability_grid(home_expected_goals, away_expected_goals, max_goals=8)`
- `calculate_total_goals_probability(grid, line, side)`
- `calculate_asian_handicap_cover_probability(grid, line, side)`

Use normalized Poisson probabilities over `0..8` goals. For quarter lines, split into two half-stakes and return expected winning stake probability where win=1, push=0.5, lose=0. This keeps Asian handicap and totals comparable to odds implied probability.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_market_probability_service.py -q`

Expected: PASS.

### Task 2: 模型推荐服务

**Files:**
- Modify: `src/icewine_prediction/recommendation_service.py`
- Test: `tests/test_model_recommendation_service.py`

- [ ] **Step 1: Write the failing tests**

```python
from decimal import Decimal

from icewine_prediction.feature_service import MatchOddsFeatures, OddsMarketAggregate
from icewine_prediction.model_training_service import BaselineResultModel
from icewine_prediction.recommendation_service import build_model_recommendations_from_features


def _aggregate(mean: Decimal | None, disagreement: Decimal | None = Decimal("0.00")):
    return OddsMarketAggregate(
        sample_count=6 if mean is not None else 0,
        mean=mean,
        median=mean,
        minimum=mean,
        maximum=mean,
        disagreement=disagreement,
    )


def _features() -> MatchOddsFeatures:
    return MatchOddsFeatures(
        match_id=1,
        bookmaker_count=8,
        asian_handicap=_aggregate(Decimal("-0.25")),
        home_odds=_aggregate(Decimal("1.95")),
        away_odds=_aggregate(Decimal("1.95")),
        total_line=_aggregate(Decimal("2.5")),
        over_odds=_aggregate(Decimal("2.05")),
        under_odds=_aggregate(Decimal("1.85")),
    )


def test_model_recommendations_generate_handicap_and_total_markets():
    recommendations = build_model_recommendations_from_features(
        features=_features(),
        model=BaselineResultModel(
            home_expected_goals=Decimal("1.80"),
            away_expected_goals=Decimal("1.00"),
        ),
    )

    assert len(recommendations) == 2
    assert recommendations[0].market_type == "asian_handicap"
    assert recommendations[0].side == "home"
    assert recommendations[0].edge > Decimal("0")
    assert recommendations[1].market_type == "total_goals"
    assert recommendations[1].side in {"over", "under", "watch"}


def test_model_recommendations_watch_when_market_line_is_missing():
    features = MatchOddsFeatures(
        match_id=1,
        bookmaker_count=8,
        asian_handicap=_aggregate(None),
        home_odds=_aggregate(None),
        away_odds=_aggregate(None),
        total_line=_aggregate(None),
        over_odds=_aggregate(None),
        under_odds=_aggregate(None),
    )

    recommendations = build_model_recommendations_from_features(
        features=features,
        model=BaselineResultModel(
            home_expected_goals=Decimal("1.30"),
            away_expected_goals=Decimal("1.10"),
        ),
    )

    assert all(recommendation.side == "watch" for recommendation in recommendations)
    assert all("missing_market_odds" in recommendation.risk_tags for recommendation in recommendations)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: FAIL because `build_model_recommendations_from_features` is missing.

- [ ] **Step 3: Write minimal implementation**

Add `build_model_recommendations_from_features(features, model)`:
- Build score grid from `BaselineResultModel`.
- Asian handicap: compare home cover probability vs away cover probability using `features.asian_handicap.mean`; choose stronger side.
- Totals: compare over vs under probability using `features.total_line.mean`; choose stronger side.
- Market implied probability: `1 / decimal_odds`, rounded to four decimals.
- Similar backtest ROI placeholder: `Decimal("0.05")` until historical backtest is implemented.
- Risk tags: `low_bookmaker_count`, `missing_market_odds`, `handicap_disagreement_high`, `total_disagreement_high`.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: PASS.

### Task 3: 模型推荐 CLI 预览

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_model_recommendations_cli.py`

- [ ] **Step 1: Write the failing tests**

```python
from decimal import Decimal

from typer.testing import CliRunner

from icewine_prediction.cli import app
from icewine_prediction.model_training_service import BaselineResultModel


def test_recommendations_model_preview_command_exists(monkeypatch):
    runner = CliRunner()
    monkeypatch.setattr("icewine_prediction.cli.list_training_samples", lambda session, limit: ["sample"] * limit)
    monkeypatch.setattr(
        "icewine_prediction.cli.train_baseline_result_model",
        lambda samples: BaselineResultModel(
            home_expected_goals=Decimal("1.50"),
            away_expected_goals=Decimal("1.10"),
        ),
    )
    monkeypatch.setattr("icewine_prediction.cli.list_upcoming_match_odds_features", lambda session, start_time, hours: [])

    result = runner.invoke(app, ["recommendations", "model-preview", "--hours", "24", "--sample-limit", "10"])

    assert result.exit_code == 0
```

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendations_cli.py -q`

Expected: FAIL because `model-preview` command is missing.

- [ ] **Step 3: Write minimal implementation**

Add CLI command:
- `recommendations model-preview --hours 24 --sample-limit 1000`
- Load training samples.
- Train baseline model.
- Fetch upcoming odds features.
- Build model recommendations and display with existing `format_recommendation_line`.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendations_cli.py -q`

Expected: PASS.

### Task 4: Verification and commit

**Files:**
- Add all files from previous tasks.

- [ ] **Step 1: Run targeted tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_market_probability_service.py tests/test_model_recommendation_service.py tests/test_model_recommendations_cli.py -q`

Expected: PASS.

- [ ] **Step 2: Run full tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests -q`

Expected: PASS.

- [ ] **Step 3: Run real CLI smoke**

Run:
`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; C:\ProgramData\anaconda3\python.exe -m icewine_cli recommendations model-preview --hours 24 --sample-limit 500`

Expected: Exit code 0 and Chinese match/recommendation lines when upcoming odds features exist.

- [ ] **Step 4: Check secrets and diff**

Run:
`Select-String -Path src/**/*.py,tests/**/*.py,docs/**/*.md -Pattern '真实API密钥|API_FOOTBALL_KEY=' -ErrorAction SilentlyContinue`

Expected: No real API key. Environment variable names and placeholder docs are acceptable.

- [ ] **Step 5: Commit**

Run:
`git add src/icewine_prediction/market_probability_service.py src/icewine_prediction/recommendation_service.py src/icewine_prediction/cli.py tests/test_market_probability_service.py tests/test_model_recommendation_service.py tests/test_model_recommendations_cli.py docs/计划/20260523-184405-模型盘口推荐接入实现计划.md`

Then:
`git commit -m "接入模型盘口推荐预览"`

Expected: Commit succeeds with Chinese message.

---

## Self-Review

- Spec coverage: 覆盖比分概率矩阵、亚盘概率、大小球概率、模型推荐、CLI 预览、验证与提交。
- Placeholder scan: 计划中没有 TBD/TODO；`similar_backtest_roi` 明确为当前阶段固定占位值，后续由历史回测替换。
- Type consistency: `BaselineResultModel`、`MatchOddsFeatures`、`Recommendation` 均沿用现有接口；新增函数名在测试和实现任务中保持一致。
