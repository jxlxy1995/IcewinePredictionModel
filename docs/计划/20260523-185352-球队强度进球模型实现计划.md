# 球队强度进球模型实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将当前全局 Poisson 基线升级为球队级进攻/防守强度模型，让每场比赛拥有按主客队调整后的期望进球。

**Architecture:** 在 `model_training_service.py` 中新增球队强度模型，继续使用 `TrainingSample` 的北京时间比赛时间和时间衰减权重。推荐层仍复用盘口概率服务和现有信心等级，只把输入模型从全局均值换成“针对本场比赛生成的 `BaselineResultModel`”。

**Tech Stack:** Python 3.12、Decimal、pytest、Typer、SQLAlchemy、Asia/Shanghai 时间。

---

### Task 1: 球队强度训练模型

**Files:**
- Modify: `src/icewine_prediction/model_training_service.py`
- Test: `tests/test_team_strength_model.py`

- [ ] **Step 1: Write the failing tests**

```python
from datetime import datetime
from decimal import Decimal
from zoneinfo import ZoneInfo

from icewine_prediction.model_training_service import train_team_strength_goal_model
from icewine_prediction.training_sample_service import TrainingSample


def _sample(match_id, home_team, away_team, home_score, away_score):
    return TrainingSample(
        match_id=match_id,
        source_match_id=str(match_id),
        league_name="La Liga",
        home_team_name=home_team,
        away_team_name=away_team,
        kickoff_time=datetime(2025, 5, match_id, 3, 0, tzinfo=ZoneInfo("Asia/Shanghai")),
        home_score=home_score,
        away_score=away_score,
        match_result="home_win" if home_score > away_score else "away_win" if home_score < away_score else "draw",
        total_goals=home_score + away_score,
        asian_handicap_line=None,
        home_handicap_result=None,
        away_handicap_result=None,
        total_line=None,
        over_result=None,
        under_result=None,
        has_odds_snapshot=False,
        sample_age_days=300,
        time_decay_weight=Decimal("1.00"),
    )


def test_team_strength_model_predicts_match_expected_goals():
    model = train_team_strength_goal_model(
        [
            _sample(1, "Strong", "Weak", 3, 0),
            _sample(2, "Strong", "Average", 2, 1),
            _sample(3, "Average", "Weak", 2, 0),
            _sample(4, "Weak", "Strong", 0, 2),
        ]
    )

    match_model = model.predict_match_result_model("Strong", "Weak")

    assert match_model.home_expected_goals > match_model.away_expected_goals
    assert match_model.home_expected_goals >= Decimal("1.50")


def test_team_strength_model_falls_back_for_unknown_team():
    model = train_team_strength_goal_model(
        [
            _sample(1, "Home", "Away", 1, 1),
            _sample(2, "Away", "Home", 0, 1),
        ]
    )

    match_model = model.predict_match_result_model("Unknown", "Away")

    assert match_model.home_expected_goals > Decimal("0")
    assert match_model.away_expected_goals > Decimal("0")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_team_strength_model.py -q`

Expected: FAIL because `train_team_strength_goal_model` is missing.

- [ ] **Step 3: Write minimal implementation**

Add:
- `TeamGoalStrength`
- `TeamStrengthGoalModel`
- `train_team_strength_goal_model(samples)`

Rules:
- Weighted league averages:
  - `home_goal_average`
  - `away_goal_average`
- For each team:
  - `home_attack_strength = team home goals for / league home average`
  - `home_defense_strength = team home goals against / league away average`
  - `away_attack_strength = team away goals for / league away average`
  - `away_defense_strength = team away goals against / league home average`
- Prediction:
  - home expected goals = `home_goal_average * home_team.home_attack_strength * away_team.away_defense_strength`
  - away expected goals = `away_goal_average * away_team.away_attack_strength * home_team.home_defense_strength`
- Unknown teams use neutral strength `1.00`.
- Round expected goals to two decimals.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_team_strength_model.py -q`

Expected: PASS.

### Task 2: 推荐层支持球队强度模型

**Files:**
- Modify: `src/icewine_prediction/recommendation_service.py`
- Test: `tests/test_model_recommendation_service.py`

- [ ] **Step 1: Write the failing test**

```python
def test_model_recommendations_can_use_team_specific_model():
    team_model = train_team_strength_goal_model(
        [
            _sample(1, "Strong", "Weak", 3, 0),
            _sample(2, "Weak", "Strong", 0, 2),
            _sample(3, "Strong", "Average", 2, 0),
        ]
    )

    recommendations = build_model_recommendations_from_features(
        features=_features(),
        model=team_model,
        home_team_name="Strong",
        away_team_name="Weak",
    )

    assert recommendations[0].side == "home"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: FAIL because `build_model_recommendations_from_features` does not accept team names or team model.

- [ ] **Step 3: Write minimal implementation**

Update `build_model_recommendations_from_features`:
- Accept `model: BaselineResultModel | TeamStrengthGoalModel`.
- Optional `home_team_name` and `away_team_name`.
- If model has `predict_match_result_model`, convert it to `BaselineResultModel` before calculating markets.
- Existing callers with `BaselineResultModel` keep working.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: PASS.

### Task 3: CLI 使用球队强度模型

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_model_recommendations_cli.py`

- [ ] **Step 1: Write the failing test**

Update `test_recommendations_model_preview_command_exists` to monkeypatch `train_team_strength_goal_model` instead of `train_baseline_result_model`.

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendations_cli.py -q`

Expected: FAIL because CLI does not import or call `train_team_strength_goal_model`.

- [ ] **Step 3: Write minimal implementation**

Update `recommendations model-preview`:
- Train `team_model = train_team_strength_goal_model(samples)`.
- For each feature row, pass:
  - `model=team_model`
  - `home_team_name=row.match.home_team.canonical_name`
  - `away_team_name=row.match.away_team.canonical_name`

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendations_cli.py -q`

Expected: PASS.

### Task 4: Verification and commit

**Files:**
- Add/modify all files from previous tasks.

- [ ] **Step 1: Run targeted tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_team_strength_model.py tests/test_model_recommendation_service.py tests/test_model_recommendations_cli.py -q`

Expected: PASS.

- [ ] **Step 2: Run full tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests -q`

Expected: PASS.

- [ ] **Step 3: Run real CLI smoke**

Run:
`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; C:\ProgramData\anaconda3\python.exe -m icewine_cli recommendations model-preview --hours 24 --sample-limit 500`

Expected: Exit code 0 and Chinese match/recommendation lines when upcoming odds features exist.

- [ ] **Step 4: Check secrets and diff**

Run:
`Select-String -Path src/**/*.py,tests/**/*.py,docs/**/*.md -Pattern '真实API密钥|API_FOOTBALL_KEY=' -ErrorAction SilentlyContinue`

Expected: No real API key. Environment variable names and placeholder docs are acceptable.

- [ ] **Step 5: Commit**

Run:
`git add src/icewine_prediction/model_training_service.py src/icewine_prediction/recommendation_service.py src/icewine_prediction/cli.py tests/test_team_strength_model.py tests/test_model_recommendation_service.py tests/test_model_recommendations_cli.py docs/计划/20260523-185352-球队强度进球模型实现计划.md`

Then:
`git commit -m "添加球队强度进球模型"`

Expected: Commit succeeds with Chinese message.

---

## Self-Review

- Spec coverage: 覆盖球队进攻/防守强度训练、未知球队回退、推荐层适配、CLI 使用球队模型、验证与提交。
- Placeholder scan: 没有 TBD/TODO；所有阶段均有具体测试和命令。
- Type consistency: `TeamStrengthGoalModel.predict_match_result_model()` 返回现有 `BaselineResultModel`，因此盘口概率服务和推荐服务不用重复实现。
