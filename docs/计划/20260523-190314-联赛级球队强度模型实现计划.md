# 联赛级球队强度模型实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将球队强度模型按联赛拆分训练，避免不同联赛的进球环境互相污染。

**Architecture:** 在现有 `TeamStrengthGoalModel` 外新增 `LeagueTeamStrengthGoalModel`，内部按 `TrainingSample.league_name` 分组训练球队强度模型。推荐层接收联赛名、主队名、客队名后选择对应联赛模型；如果联赛样本不足或找不到，则回退到全局球队强度模型。

**Tech Stack:** Python 3.12、Decimal、pytest、Typer、SQLAlchemy、Asia/Shanghai 时间。

---

### Task 1: 联赛级训练模型

**Files:**
- Modify: `src/icewine_prediction/model_training_service.py`
- Test: `tests/test_league_team_strength_model.py`

- [ ] **Step 1: Write the failing tests**

```python
from datetime import datetime
from decimal import Decimal
from zoneinfo import ZoneInfo

from icewine_prediction.model_training_service import train_league_team_strength_goal_model
from icewine_prediction.training_sample_service import TrainingSample


def _sample(match_id, league, home_team, away_team, home_score, away_score):
    return TrainingSample(
        match_id=match_id,
        source_match_id=str(match_id),
        league_name=league,
        home_team_name=home_team,
        away_team_name=away_team,
        kickoff_time=datetime(2025, 5, match_id, 3, 0, tzinfo=ZoneInfo("Asia/Shanghai")),
        home_score=home_score,
        away_score=away_score,
        match_result="home_win" if home_score > away_score else "away_win" if home_score < away_score else "draw",
        total_goals=home_score + away_score,
        asian_handicap_line=None,
        home_handicap_result=None,
        away_handicap_result=None,
        total_line=None,
        over_result=None,
        under_result=None,
        has_odds_snapshot=False,
        sample_age_days=300,
        time_decay_weight=Decimal("1.00"),
    )


def test_league_team_strength_model_uses_requested_league_context():
    model = train_league_team_strength_goal_model(
        [
            _sample(1, "League A", "Strong", "Weak", 4, 0),
            _sample(2, "League A", "Weak", "Strong", 0, 3),
            _sample(3, "League B", "Strong", "Weak", 1, 1),
            _sample(4, "League B", "Weak", "Strong", 1, 1),
        ]
    )

    league_a = model.predict_match_result_model("League A", "Strong", "Weak")
    league_b = model.predict_match_result_model("League B", "Strong", "Weak")

    assert league_a.home_expected_goals > league_b.home_expected_goals


def test_league_team_strength_model_falls_back_to_global_model_for_unknown_league():
    model = train_league_team_strength_goal_model(
        [
            _sample(1, "League A", "Home", "Away", 2, 1),
            _sample(2, "League A", "Away", "Home", 0, 1),
        ]
    )

    match_model = model.predict_match_result_model("Unknown League", "Home", "Away")

    assert match_model.home_expected_goals > Decimal("0")
    assert match_model.away_expected_goals > Decimal("0")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_league_team_strength_model.py -q`

Expected: FAIL because `train_league_team_strength_goal_model` is missing.

- [ ] **Step 3: Write minimal implementation**

Add:
- `LeagueTeamStrengthGoalModel`
- `train_league_team_strength_goal_model(samples)`

Rules:
- Build one global `TeamStrengthGoalModel` from all samples.
- Group samples by `league_name`, train one `TeamStrengthGoalModel` per league.
- `predict_match_result_model(league_name, home_team_name, away_team_name)` uses league-specific model if present, else global model.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_league_team_strength_model.py -q`

Expected: PASS.

### Task 2: 推荐层接收联赛级模型

**Files:**
- Modify: `src/icewine_prediction/recommendation_service.py`
- Test: `tests/test_model_recommendation_service.py`

- [ ] **Step 1: Write the failing test**

Add a test that calls `build_model_recommendations_from_features` with `LeagueTeamStrengthGoalModel`, `league_name`, `home_team_name`, and `away_team_name`.

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: FAIL because recommendation service does not accept `league_name`.

- [ ] **Step 3: Write minimal implementation**

Update `build_model_recommendations_from_features`:
- Accept `model: BaselineResultModel | TeamStrengthGoalModel | LeagueTeamStrengthGoalModel`.
- Optional `league_name`.
- If model is league-level, require `league_name`, `home_team_name`, `away_team_name`, then call `predict_match_result_model`.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: PASS.

### Task 3: CLI 使用联赛级模型

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_model_recommendations_cli.py`

- [ ] **Step 1: Write the failing test**

Update CLI test to monkeypatch `train_league_team_strength_goal_model`.

- [ ] **Step 2: Run test to verify it fails**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendations_cli.py -q`

Expected: FAIL because CLI does not import or call `train_league_team_strength_goal_model`.

- [ ] **Step 3: Write minimal implementation**

Update `recommendations model-preview`:
- Train `league_model = train_league_team_strength_goal_model(samples)`.
- Pass `league_name=row.match.league.name` to `build_model_recommendations_from_features`.

- [ ] **Step 4: Run test to verify it passes**

Run: `$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendations_cli.py -q`

Expected: PASS.

### Task 4: Verification and commit

**Files:**
- Add/modify all files from previous tasks.

- [ ] **Step 1: Run targeted tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_league_team_strength_model.py tests/test_model_recommendation_service.py tests/test_model_recommendations_cli.py -q`

Expected: PASS.

- [ ] **Step 2: Run full tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests -q`

Expected: PASS.

- [ ] **Step 3: Run real CLI smoke**

Run:
`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; C:\ProgramData\anaconda3\python.exe -m icewine_cli recommendations model-preview --hours 24 --sample-limit 500`

Expected: Exit code 0 and Chinese match/recommendation lines when upcoming odds features exist.

- [ ] **Step 4: Check secrets and diff**

Run:
`Select-String -Path src/**/*.py,tests/**/*.py,docs/**/*.md -Pattern '真实API密钥|API_FOOTBALL_KEY=' -ErrorAction SilentlyContinue`

Expected: No real API key. Environment variable names and placeholder docs are acceptable.

- [ ] **Step 5: Commit**

Run:
`git add src/icewine_prediction/model_training_service.py src/icewine_prediction/recommendation_service.py src/icewine_prediction/cli.py tests/test_league_team_strength_model.py tests/test_model_recommendation_service.py tests/test_model_recommendations_cli.py docs/计划/20260523-190314-联赛级球队强度模型实现计划.md`

Then:
`git commit -m "添加联赛级球队强度模型"`

Expected: Commit succeeds with Chinese message.

---

## Self-Review

- Spec coverage: 覆盖联赛分组训练、未知联赛回退、推荐层适配、CLI 使用联赛级模型、验证与提交。
- Placeholder scan: 没有 TBD/TODO；每个阶段都有明确测试与命令。
- Type consistency: 联赛模型最终仍返回 `BaselineResultModel`，盘口概率服务和推荐计算无需重复实现。
