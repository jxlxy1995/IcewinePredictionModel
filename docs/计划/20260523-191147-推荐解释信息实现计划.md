# 推荐解释信息实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在推荐结果中展示模型期望进球、模型概率、赔率隐含概率、edge 和盘口线，让 CLI 推荐具备可审阅的原因。

**Architecture:** 扩展 `Recommendation` 数据结构，新增可选解释字段；模型推荐负责填充解释字段，规则推荐和观望推荐保持兼容。CLI 格式化函数在解释字段存在时追加中文说明，不改变已有推荐命令入口。

**Tech Stack:** Python 3.12、Decimal、pytest、Typer、Asia/Shanghai 时间。

---

### Task 1: 推荐对象携带概率解释

**Files:**
- Modify: `src/icewine_prediction/recommendation_service.py`
- Test: `tests/test_recommendation_service.py`

- [ ] **Step 1: Write the failing test**

Add assertions that `build_recommendation_from_signal` stores:
- `model_probability`
- `market_implied_probability`
- `similar_backtest_roi`

- [ ] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_recommendation_service.py -q`

Expected: FAIL because `Recommendation` does not expose the new fields.

- [ ] **Step 3: Write minimal implementation**

Add fields to `Recommendation`:
- `model_probability: Decimal | None = None`
- `market_implied_probability: Decimal | None = None`
- `similar_backtest_roi: Decimal | None = None`
- `home_expected_goals: Decimal | None = None`
- `away_expected_goals: Decimal | None = None`
- `market_line: Decimal | None = None`

Populate the first three in `build_recommendation_from_signal`.

- [ ] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_recommendation_service.py -q`

Expected: PASS.

### Task 2: 模型推荐填充期望进球和盘口线

**Files:**
- Modify: `src/icewine_prediction/recommendation_service.py`
- Test: `tests/test_model_recommendation_service.py`

- [ ] **Step 1: Write the failing test**

Add assertions for model recommendations:
- `home_expected_goals`
- `away_expected_goals`
- `market_line`
- `model_probability`
- `market_implied_probability`

- [ ] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: FAIL because model recommendations do not enrich expected goals and market line yet.

- [ ] **Step 3: Write minimal implementation**

Add helper `_with_model_context(recommendation, model, market_line)` that returns a copied `Recommendation` with:
- `home_expected_goals=model.home_expected_goals`
- `away_expected_goals=model.away_expected_goals`
- `market_line=market_line`

Call it for handicap and totals model recommendations.

- [ ] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_model_recommendation_service.py -q`

Expected: PASS.

### Task 3: CLI 展示推荐解释

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_recommendations_cli.py`

- [ ] **Step 1: Write the failing test**

Extend `test_format_recommendation_line_uses_chinese_match_and_recommendation_text` to assert the line includes:
- `模型概率`
- `隐含概率`
- `edge`
- `期望进球`

- [ ] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_recommendations_cli.py -q`

Expected: FAIL because CLI does not display explanation fields.

- [ ] **Step 3: Write minimal implementation**

Update `_format_recommendation_part`:
- Append `线 {market_line}` when present.
- Append `模型概率 {model_probability}` when present.
- Append `隐含概率 {market_implied_probability}` when present.
- Append `edge {edge}` when model probability exists.
- Append `期望进球 {home_expected_goals}-{away_expected_goals}` when both expected goals are present.

- [ ] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_recommendations_cli.py -q`

Expected: PASS.

### Task 4: Verification and commit

**Files:**
- Add/modify all files from previous tasks.

- [ ] **Step 1: Run targeted tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_recommendation_service.py tests/test_model_recommendation_service.py tests/test_recommendations_cli.py -q`

Expected: PASS.

- [ ] **Step 2: Run full tests**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests -q`

Expected: PASS.

- [ ] **Step 3: Run real CLI smoke**

Run:
`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; C:\ProgramData\anaconda3\python.exe -m icewine_cli recommendations model-preview --hours 24 --sample-limit 500`

Expected: Exit code 0, and model recommendations include explanation fragments.

- [ ] **Step 4: Check secrets and diff**

Run:
`Select-String -Path src/**/*.py,tests/**/*.py,docs/**/*.md -Pattern '真实API密钥|API_FOOTBALL_KEY=' -ErrorAction SilentlyContinue`

Expected: No real API key. Environment variable names and placeholder docs are acceptable.

- [ ] **Step 5: Commit**

Run:
`git add src/icewine_prediction/recommendation_service.py src/icewine_prediction/cli.py tests/test_recommendation_service.py tests/test_model_recommendation_service.py tests/test_recommendations_cli.py docs/计划/20260523-191147-推荐解释信息实现计划.md`

Then:
`git commit -m "添加推荐解释信息"`

Expected: Commit succeeds with Chinese message.

---

## Self-Review

- Spec coverage: 覆盖推荐对象字段、模型推荐填充、CLI 中文展示、验证与提交。
- Placeholder scan: 没有 TBD/TODO；字段、命令、文件路径均明确。
- Type consistency: 新字段为可选字段，旧规则推荐和旧测试可兼容。
