# edge分桶复盘报表实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在推荐复盘报表中按 edge 区间统计 ROI，为后续相似历史表现和出手门槛提供基础。

**Architecture:** 扩展 `RecordReport`，新增 `by_edge_bucket` 分组；复用现有 `RecordGroupSummary` 统计手数、盈亏和 ROI。CLI `records report` 新增“按edge”一行，不改变推荐记录和结算逻辑。

**Tech Stack:** Python 3.12、Decimal、pytest、Typer。

---

### Task 1: 报表服务增加 edge 分桶

**Files:**
- Modify: `src/icewine_prediction/record_service.py`
- Test: `tests/test_record_report_service.py`

- [x] **Step 1: Write failing test**

Add settled records with edge values:
- `0.0200` -> `0.00-0.03`
- `0.0500` -> `0.03-0.06`
- `0.0800` -> `0.06-0.10`
- `0.1200` -> `0.10+`

Assert `build_record_report(session).by_edge_bucket` contains these buckets.

- [x] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_record_report_service.py -q`

Expected: FAIL because `RecordReport` has no `by_edge_bucket`.

- [x] **Step 3: Implement minimal code**

Add:
- `edge_bucket_for_value(edge: Decimal) -> str`
- `by_edge_bucket` to `RecordReport`
- group settled records by bucket.

Buckets:
- `< 0.03`: `0.00-0.03`
- `< 0.06`: `0.03-0.06`
- `< 0.10`: `0.06-0.10`
- otherwise: `0.10+`

- [x] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_record_report_service.py -q`

Expected: PASS.

### Task 2: CLI 展示 edge 分桶

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_records_cli.py`

- [x] **Step 1: Write failing test**

Extend `format_record_report` test to assert:
- output contains `按edge`
- output contains `0.10+`

- [x] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_records_cli.py -q`

Expected: FAIL because CLI does not render edge buckets.

- [x] **Step 3: Implement minimal code**

Update `format_record_report` to append:
`_format_record_groups("按edge", report.by_edge_bucket)`

- [x] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_records_cli.py -q`

Expected: PASS.

### Task 3: Verification and commit

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests -q`

Run real smoke:
`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; C:\ProgramData\anaconda3\python.exe -m icewine_cli records report`

Commit:
`git commit -m "添加edge分桶复盘报表"`

---

## Self-Review

- Spec coverage: 覆盖服务统计、CLI 展示、测试、验证与提交。
- Placeholder scan: 没有 TBD/TODO。
- Type consistency: 复用 `RecordGroupSummary`，不改数据库结构。
