# 相似历史表现服务实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增相似历史表现服务和 CLI 查询命令，用已结算推荐记录观察不同条件组合下的 ROI 表现。

**Architecture:** 新建 `historical_performance_service.py` 聚合已结算 `RecommendationRecord`，复用 `RecordGroupSummary` 与 `edge_bucket_for_value`。CLI 新增 `records performance` 命令，参数只做过滤，展示逻辑放在格式化函数中。

**Tech Stack:** Python 3.12、SQLAlchemy、Decimal、Typer、pytest。

---

### Task 1: 历史表现服务

**Files:**
- Create: `src/icewine_prediction/historical_performance_service.py`
- Test: `tests/test_historical_performance_service.py`

- [x] **Step 1: Write failing test**

创建包含多条已结算推荐和一条待结算推荐的样本，断言服务只统计已结算记录，并能按 `market_type`、`side`、`edge_bucket` 过滤。

- [x] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_historical_performance_service.py -q`

Expected: FAIL because service module does not exist.

- [x] **Step 3: Implement minimal service**

新增：

- `HistoricalPerformanceFilters`
- `HistoricalPerformanceReport`
- `build_historical_performance_report(session, filters)`

服务只读取 `status = settled` 的推荐记录，并输出总览与各维度分组。

- [x] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_historical_performance_service.py -q`

Expected: PASS.

### Task 2: CLI 查询命令

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_records_cli.py`

- [x] **Step 1: Write failing test**

新增格式化测试，断言输出包含：

- `历史样本`
- `按edge`
- `按方向`
- `ROI`

- [x] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_records_cli.py -q`

Expected: FAIL because formatter or CLI command does not exist.

- [x] **Step 3: Implement CLI**

新增：

- `format_historical_performance_report`
- `records performance` 命令
- 可选参数：`--market-type`、`--side`、`--league-name`、`--edge-bucket`、`--confidence-grade`

- [x] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_records_cli.py -q`

Expected: PASS.

### Task 3: Verification and commit

- [x] Run full tests:

`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests -q`

- [x] Run CLI smoke:

`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; C:\ProgramData\anaconda3\python.exe -m icewine_cli records performance`

- [x] Secret scan:

`Select-String -Path src/**/*.py,tests/**/*.py,docs/**/*.md -Pattern '真实API密钥|API_FOOTBALL_KEY=' -ErrorAction SilentlyContinue`

- [x] Commit:

`git commit -m "添加相似历史表现查询"`

---

## Self-Review

- Spec coverage: 覆盖服务层、CLI 查询、测试、验证与提交。
- Placeholder scan: 没有 TBD/TODO。
- Scope check: 不改推荐逻辑，不改数据库结构。
- Type consistency: 复用已有 `RecordGroupSummary` 和 edge 分桶规则。
