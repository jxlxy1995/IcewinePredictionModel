# 联赛白名单与覆盖率报告实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 扩展主流联赛白名单和中文展示名，并新增本地历史数据覆盖率报告 CLI。

**Architecture:** `config/leagues.yaml` 维护 API 联赛白名单，`config/display_names.yaml` 维护中文展示名。新增 coverage service 从本地 `Match` 与 `OddsSnapshot` 统计覆盖情况，CLI 只负责格式化展示。

**Tech Stack:** Python 3.12、SQLAlchemy、Typer、pytest、YAML。

---

### Task 1: 配置主流联赛白名单与中文译名

**Files:**
- Modify: `config/leagues.yaml`
- Modify: `config/display_names.yaml`
- Test: `tests/test_settings.py`
- Test: `tests/test_display_service.py`

- [x] **Step 1: Write failing tests**

断言设置中包含：

- `2. Bundesliga`，id 为 `79`
- `J1 League`，id 为 `98`
- `K League 1`，id 为 `292`

断言中文展示名包含：

- `2. Bundesliga -> 德乙`
- `3. Liga -> 德丙`
- `J1 League -> 日职联`

- [x] **Step 2: Run tests to verify they fail**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_settings.py tests/test_display_service.py -q`

Expected: FAIL because current config is incomplete.

- [x] **Step 3: Update config**

扩展 `config/leagues.yaml` 和 `config/display_names.yaml`。

- [x] **Step 4: Run tests to verify they pass**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_settings.py tests/test_display_service.py -q`

Expected: PASS.

### Task 2: 本地历史覆盖率服务

**Files:**
- Create: `src/icewine_prediction/history_coverage_service.py`
- Test: `tests/test_history_coverage_service.py`

- [x] **Step 1: Write failing test**

构造一个联赛、两场比赛和赔率快照，断言覆盖率报告能统计：

- 总比赛数
- 已完赛数
- 有比分数
- 有赔率比赛数
- 有亚盘比赛数
- 有大小球比赛数
- 覆盖率

- [x] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_history_coverage_service.py -q`

Expected: FAIL because service module does not exist.

- [x] **Step 3: Implement minimal service**

新增：

- `LeagueCoverageSummary`
- `build_history_coverage_report(session, season=None)`

- [x] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_history_coverage_service.py -q`

Expected: PASS.

### Task 3: CLI 展示覆盖率

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_history_coverage_cli.py`

- [x] **Step 1: Write failing test**

断言 `history coverage --help` 存在，并断言格式化函数输出中文字段：

- `总比赛`
- `赔率覆盖`
- `亚盘覆盖`
- `大小球覆盖`

- [x] **Step 2: Run test to verify it fails**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_history_coverage_cli.py -q`

Expected: FAIL because CLI command does not exist.

- [x] **Step 3: Implement CLI**

新增 `history_app` 和 `history coverage` 命令，支持 `--season` 参数。

- [x] **Step 4: Run test to verify it passes**

Run:
`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests/test_history_coverage_cli.py -q`

Expected: PASS.

### Task 4: Verification and commit

- [x] Run full tests:

`$env:PYTHONPATH='src'; C:\ProgramData\anaconda3\python.exe -m pytest tests -q`

- [x] Run CLI smoke:

`$env:PYTHONPATH='src'; $env:PYTHONIOENCODING='utf-8'; C:\ProgramData\anaconda3\python.exe -m icewine_cli history coverage --season 2025`

- [x] Secret scan:

`Select-String -Path src/**/*.py,tests/**/*.py,docs/**/*.md,config/*.yaml -Pattern '真实API密钥|API_FOOTBALL_KEY=' -ErrorAction SilentlyContinue`

- [x] Commit:

`git commit -m "添加联赛白名单与历史覆盖率报告"`

---

## Self-Review

- Spec coverage: 覆盖配置、中文展示、服务、CLI、验证与提交。
- Placeholder scan: 没有 TBD/TODO。
- Scope check: 不自动批量拉取 API，不改数据库结构。
- Type consistency: 报告服务和 CLI 字段一致。
