# Skellam 净胜球模型实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 Skellam 净胜球模型，支持直接计算亚盘主队和客队覆盖概率。

**Architecture:** 新建 `skellam_model_service.py`，使用 `scipy.stats.skellam` 计算净胜球概率。模型不读取数据库，不依赖赔率回填口径；CLI 提供一个纯参数调试命令。

**Tech Stack:** Python 3.11+、Decimal、scipy、pytest、Typer。

---

### Task 1: Skellam 概率服务

**Files:**
- Create: `src/icewine_prediction/skellam_model_service.py`
- Test: `tests/test_skellam_model_service.py`

- [ ] **Step 1: Write failing tests**

Add tests proving:

- Stronger home expected goals gives higher positive margin probability.
- Asian handicap probability supports half and quarter lines.
- Home and away cover probabilities are symmetrical for the same line.

- [ ] **Step 2: Run tests to verify they fail**

Run:

```powershell
python -m pytest tests/test_skellam_model_service.py -q
```

Expected: FAIL because `skellam_model_service.py` is missing.

- [ ] **Step 3: Write minimal implementation**

Add:

- `SkellamMarginModel`
- `SkellamHandicapProbability`
- quarter-line splitting
- settlement scoring using Skellam PMF

- [ ] **Step 4: Run tests to verify they pass**

Run:

```powershell
python -m pytest tests/test_skellam_model_service.py -q
```

Expected: PASS.

### Task 2: CLI 调试命令

**Files:**
- Modify: `src/icewine_prediction/cli.py`
- Test: `tests/test_models_cli.py`

- [ ] **Step 1: Write failing test**

Add a test for:

```powershell
icewine models skellam-handicap --home-eg 1.50 --away-eg 1.10 --line -0.25
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
python -m pytest tests/test_models_cli.py::test_models_skellam_handicap_outputs_probabilities -q
```

Expected: FAIL because the command is missing.

- [ ] **Step 3: Write minimal implementation**

Import `SkellamMarginModel`, calculate handicap probabilities, and print Chinese labels.

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
python -m pytest tests/test_models_cli.py::test_models_skellam_handicap_outputs_probabilities -q
```

Expected: PASS.

### Task 3: Verification and log

**Files:**
- Modify: `docs/团队协作/20260525-Codex工作日志.md`

- [ ] **Step 1: Run targeted tests**

Run:

```powershell
python -m pytest tests/test_skellam_model_service.py tests/test_models_cli.py -q
```

Expected: PASS.

- [ ] **Step 2: Run full tests**

Run:

```powershell
python -m pytest tests -q
```

Expected: PASS.

- [ ] **Step 3: Run lint for touched files**

Run:

```powershell
python -m ruff check src tests/test_skellam_model_service.py tests/test_models_cli.py
```

Expected: PASS for touched files.

- [ ] **Step 4: Update collaboration log**

Add the Skellam work summary, verification result, and commit status to `docs/团队协作/20260525-Codex工作日志.md`.

## Self-Review

- Spec coverage: 覆盖 Skellam 净胜球分布、亚盘概率、CLI 调试命令、验证和协作日志。
- Placeholder scan: 没有未决占位；每个任务都有文件、命令和期望结果。
- Type consistency: 概率输出使用 `Decimal`，盘口输入沿用现有 `Decimal` 口径。
