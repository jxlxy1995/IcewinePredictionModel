# 历史赔率训练样本抽取实现计划

日期：`2026-05-27`

## 范围

本轮新增一个只读抽取层，把 `historical_odds_snapshots` 聚合成盘口训练样本，不改回填器、不训练模型、不新增数据库表。

## 任务

1. 新增 `tests/test_historical_training_sample_service.py`。
   - 构造一场完赛比赛。
   - 写入 `pinnacle` 的亚盘和大小球 outcome 快照。
   - 验证服务能按 `24h/12h/6h/3h/1h/15m/close` 抽取锚点。
   - 验证缺失锚点、薄历史样本、赛后快照过滤。

2. 新增 `src/icewine_prediction/historical_training_sample_service.py`。
   - 定义锚点和样本 dataclass。
   - 读取 `HistoricalOddsSnapshot`。
   - 按 match / market / bookmaker / line / snapshot_time 配对 outcome。
   - 按固定锚点选择最近可用配对。
   - 计算 implied probability、overround、盘口变化、质量标签。

3. 新增 CLI 预览命令。
   - 命令：`samples historical-odds-preview`
   - 参数：`--season`、`--limit`、`--bookmaker`
   - 输出每条样本的比赛、市场、锚点覆盖、盘口变化和质量标签。

4. 更新团队工作日志。
   - 说明样本抽取规则。
   - 写清楚不改旧训练样本服务、不训练模型、不落库。
   - 记录提交号和验证命令。

5. 验证。
   - `C:\Python312\python.exe -m pytest -q`
   - `C:\Python312\python.exe -m ruff check src/icewine_prediction/historical_training_sample_service.py src/icewine_prediction/cli.py tests/test_historical_training_sample_service.py tests/test_samples_cli.py`
